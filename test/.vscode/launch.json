{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/src/vendor/facade/ignition/package.json",
                "localRoot": "${workspaceFolder}/src/vendor/facade/ignition"
            }
        }
    ]
}